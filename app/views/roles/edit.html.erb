<%= render 'form' %>

<div class="col-md-10 pull-left">
<center>
  <div class="btn-toolbar">
    <div class="btn-group ">
      <%= link_to t('edit'), edit_role_path(@role), class: 'btn btn-info' %>
    </div>
    <div class="btn-group ">
    <%= link_to t('back'), roles_path, class: 'btn btn-info' %>
  </div>
  </div>
</center>    
</div>