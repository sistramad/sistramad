<div class="container"> 
  <div class="well"> 
    <div>
      <p><strong> <h3 class="text-center background-image: asset-url('work.jpg') background-size: cover">Listado de solicitudes: </h3> </strong></p>
    </div>
    <br>
      <div class="table-responsive">
        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>Usuario</th>
              <th>Nombre</th>
              <th>Cédula</th>
              <th>Correo electronico</th>
              <th>Solicitud </th>
              <th>Fecha creacion</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% @professors_transfers.each do |professors_transfer| %>
              <tr>
                <td><%= professors_transfer.user.username %></td>
                <td><%= professors_transfer.user.first_name%> <%= professors_transfer.user.last_name%></td>
                <td><%= professors_transfer.user.identification_document %></td>
                <td><%= professors_transfer.user.email %></td>
                <td><%= professors_transfer.name %> </td>
                <td><%= professors_transfer.created_at.strftime("%Y-%m-%d") %></td>
                <td>
                  <div class="btn-toolbar">
                    <div class="btn-group btn-group-xs">
                      <%= link_to 'Consultar', request_manager_path(professors_transfer) , class: 'btn btn-primary list' %>
                    </div>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <%= paginate @professors_transfers %></br>
      <%= page_entries_info @professors_transfers %>
  </div>
</div>