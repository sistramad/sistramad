<%= render 'form' %>

<div class="col-md-3 col-md-offset-1 pull-left">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to t('edit'), @reference, class: 'btn btn-info'%>
      <%= link_to t('back'), references_path, class: 'btn btn-info' %>
    </div>
  </div>
</div>
