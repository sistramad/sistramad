<%= render 'form_edit' %>

<div class="col-md-3 pull-left">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to t('edit'), @employee, class: 'btn btn-info'%>
      <%= link_to t('back'), employees_path, class: 'btn btn-info' %>
    </div>
  </div>
</div>