<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-info">
      <div class="panel-heading center"><%= t('formalities_document') %></div>
      <div class="panel-body">
        <%= simple_form_for @formalities_document, :html => {:multipart => true, :class => "form-horizontal"},
                            wrapper: :horizontal_file_input_sistramad,
                            wrapper_mappings: {
                                file: :horizontal_file_input_sistramad
                            } do |f| %>
            <%= f.error_notification %>

            <% if @formalities_document.errors.any? %>
                <div id="error_explanation">
                  <h2><%= pluralize(@formalities_document.errors.count, "error") %> prohibited this user from being saved:</h2>

                  <ul>
                    <% @formalities_document.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
            <% end %>

            <div class="row">
              <div class="col-xs-12 col-sm-12 col-md-9">
    				<%= f.association :formalities_master, :label => t("formalities_master"), :placeholder => t("name") %>
    				<%= f.association :document, :label => t("requirements"), :placeholder => t("name") %>
  			  </div>
  			  <center>
              <div class="col-md-12">
                <%= f.button :submit, value: t('form.send'), class: ' btn btn-info' %>
              </div>
              </center>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

