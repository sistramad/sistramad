<div class="well"> 
  <div>
    <p><strong> <h3 class="text-center">Programas de Formación: </h3> </strong></p>
  </div>
  <br>
  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Solicitud </th>
          <th>Código</th>
          <th>Fecha creación</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% @procedures.each do |procedure| %>
          <tr>
            <td><%= procedure.name%> </td>
            <td><%= procedure.code %></td>
            <td><%= procedure.created_at.strftime("%Y-%m-%d") %></td>
            <td>
              <% if procedure.code == "T-SLR204" %>
                <div class="btn-toolbar">
                  <div class="btn-group btn-group-xs">
                    <%= link_to 'Consultar', license_path(procedure), class: 'btn btn-primary list' %>
                    <%= link_to 'Cancelar', license_path(procedure), method: :delete, data: {confirm: '¿Está seguro de eliminar solicitud?'}, class: 'btn btn-danger list' %>
                  </div>
                </div>
              <% elsif procedure.code == "T-SPL205" %>
                <div class="btn-toolbar">
                  <div class="btn-group btn-group-xs">
                    <%= link_to 'Consultar', delay_license_path(procedure), class: 'btn btn-primary list' %>
                    <%= link_to 'Cancelar', delay_license_path(procedure), method: :delete, data: {confirm: '¿Está seguro de eliminar solicitud?'}, class: 'btn btn-danger list' %>
                  </div>
                </div>
              <% elsif procedure.code == "T-SRL206" %>
                <div class="btn-toolbar">
                  <div class="btn-group btn-group-xs">
                    <%= link_to 'Consultar' ,license_reincorporation_path(procedure), class: 'btn btn-primary list' %>
                    <%= link_to 'Cancelar', license_reincorporation_path(procedure), method: :delete, data: {confirm: '¿Está seguro de eliminar solicitud?'}, class: 'btn btn-danger list' %>
                  </div>
                </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>



