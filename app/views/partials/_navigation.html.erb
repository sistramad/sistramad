<header>
  <% if current_user %>
      <nav class="navbar navbar-default" role="navigation">
        <div class="container">
          <div class="navbar-header">
            <!--<%= link_to "Iniciar sesión", new_user_session_path %>-->
            <%= link_to 'Home', :authenticated_root, class: 'navbar-brand' %>

            <% if current_user.has_role? :admin %>
                <%= link_to "References", references_path, class: 'navbar-brand' %>
                <%= link_to "Reference lists", reference_lists_path, class: 'navbar-brand' %>
                <%= link_to "Users", users_path, class: 'navbar-brand' %>
                <%= link_to "Roles", roles_path, class: 'navbar-brand' %>
                <%= link_to "Employees", employees_path, class: 'navbar-brand' %>
                <%= link_to "Faculties", faculties_path, class: 'navbar-brand' %>
            <% end %>
          </div>

          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Menu <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete %></li>
                <li class="divider"></li>
                <% if current_user.has_role? :admin %>
                  <li><%= link_to 'Invitar', new_user_invitation_path %></li>
                <% end %>
              </ul>
            </li>
          </ul>

        </div>
      </nav>
  <% end %>
</header>