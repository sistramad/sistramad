<p id="notice"><%= notice %></p>

<div class="jumbotron text-center">
  <h1>Solicitar Año Sabatico:</h1>
  <%= link_to new_procedure_path(code: 'T-AS100'), class: 'btn btn-info btn-sm list' do %>
    <i class="glyphicon glyphicon-plus"></i> Nueva Solicitud
  <% end %>
</div>

<%= render 'procedure' %>
