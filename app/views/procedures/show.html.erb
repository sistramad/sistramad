<div class="well">
  <div class="container">
    <div class="row">
      <div class="col-md-12 text-center">
        <h2> <strong> Información de la solicitud: <strong></h1>
      </div>
    </div>
    <div class="row">
      <%= render 'basic_info'%>
      <% if @procedure.in_draft? %>
        <div class="col-md-6">
          <br>
          <ul>
            <h4>
              <p> <li> Debe cargar todos los recaudos.</li> </p>
              <p> <li> La solicitud tienes que hacerse en los primeros 3 meses del año en curso. </li></p>
              <p> <li> Recibirá un correo electronico con las instrucciones a seguir una vez su solicitud haya sido evaludada.</li> </p>
            </h4>
          </ul>
        </div>
      <% elsif @procedure.in_progress? and @procedure.name == 'Año Sabatico'  %>
        <%= render 'sabbatical_year_control_panel' %>  
      <% end %>
    </div>
    <br>
  </div>
  <div class="text-center">
    <% if @procedure.in_draft? %>
      <%= link_to validate_procedure_path(@procedure), class: 'btn btn-success btn-lg list' do %>
        <i class="glyphicon glyphicon-cog"></i> Solicitar evaluacion de recaudos
      <% end %>
    <% end %>
  </div>
</div>

<% if @procedure.approved? %>
  <%= render 'extra_requests'%>  
<% end %>
  <%= render 'extra_requests'%>  
<br>

<% if @procedure.in_draft? %>
  <%= render 'requirement_list' %>
<% else %>
  <%= render 'steps_list' %>
<% end %>


  
